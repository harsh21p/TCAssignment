package com.assignment.tcimageapp.core

enum class PhotosSortOption {
    DEFAULT,          // original order (API order / id)
    AUTHOR_ASC,
    AUTHOR_DESC,
    WIDTH_ASC,
    WIDTH_DESC,
    HEIGHT_ASC,
    HEIGHT_DESC
}